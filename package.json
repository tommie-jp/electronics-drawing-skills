{
  "name": "electronics-drawing-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "markdownlint '**/*.md' --ignore node_modules",
    "check": "node scripts/check.mjs",
    "all": "npm run lint && npm run check"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.49.1",
    "yaml": "^2.9.0"
  }
}
